# 03 -- the two RUNNER-SIDE guards, provoked
# Captured 2026-08-07T23:29:21Z. TRANSCRIBED: the commands were really run and these are their real
#   outputs, but the `$ ` prompt lines are hand-written labels -- this is not a
#   verbatim terminal capture. Files 04-08, 10 and 11 ARE verbatim captures.

## 3a. OPA_MIN_ROWS floor: demand 10263 from a 10262-row extract
extract has 10262 data row(s), fewer than the expected minimum of 10263. A truncated or partly-scraped extract looks exactly like this, and on a FIRST load into an empty mirror no database guard would catch it. Re-extract, or lower OPA_MIN_ROWS deliberately if the source really did shrink.
EXIT=1  (1 = refused, as required)

## 3b. OPA_MIN_ROWS is MANDATORY for --apply: omit it entirely
OPA_MIN_ROWS is required for --apply. State how many data rows you expect AT MINIMUM. The database's shrink band only fires once rows are already stored, so the FIRST load into an empty mirror has no floor without this.
EXIT=1  (1 = refused, as required)
