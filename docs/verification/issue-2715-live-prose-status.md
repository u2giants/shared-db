# Issue 2715 live prose-status verification

This prose-only pull request verifies that merged `main` grants the required
merge authorization without entering any database author, reviewer, preview,
merge, or production lane. The pull request itself is the live evidence.
