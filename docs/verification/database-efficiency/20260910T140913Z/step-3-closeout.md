# Step 3 closeout — rebuild and projection churn

**Plan / issue:** `plan_database_efficiency_and_api_security.md` Step 3 / #2326
**Recorded:** 2026-09-10T14:09:13Z
**Scope of this closeout:** repository evidence reconciliation only. It performs no database interaction and contains no row values, credentials, or private source material.

## Result

Step 3's verification gate is met only in the explicit partial scope certified below: five of its seven requirements — named caller, trigger/schedule, input size, concurrency pattern and timestamped cost — are met for all five operations, while the changed-row-count and query-plan requirements are **not** met as literally written.
The remaining open and unmeasured items are named in "What is still not proven".
The original cross-repository call graph established every named caller, schedule or trigger, concurrency path, input bound, and timestamped production-read-only cost for all five operations.
Read-side query plans were captured for three of them (`rebuild_style_groups_batch`, `clear_style_group_batch` and `refresh_style_group_counts_batch`); the call graph records no query plan of either kind for `refresh_style_guide_matviews` or `sync_asset_effective_tags`, where line 546 reads `n/a`.
The initially open changed-row questions for the effective-tag projection, the style-group counts and the style-group rebuild were then resolved through the independently governed, contract-tested migrations that those measurements justified, each cited below by its migration file, contract tests, pull request and merge commit.
This closeout makes no claim about those changes' preview-rehearsal or production-authorization records; it did not examine them, and for PR #2423 the repository's own record is explicitly the opposite of a clean rehearsal (`HANDOFF.d/2026-09-06T1330Z-edge-dev-2-claude-orch-2404-closeout.md` lines 30-33 record that PR #2423 merged to `main` **without** its preview rehearsal, carried as issue #2433, and `HANDOFF.d/2026-09-07T1634Z-edge-dev-codex-five-issue-closeout.md` §4 records a first production attempt stopping before SQL because the prior preview artifact came from a different exact `main` commit, production being reached only through historical recovery in `apply` mode). That §4 names no pull request at all: the string "2423" does not occur anywhere in that file. It attributes that production sequence to the migration file `20260906035323_style_group_rebuild_guard_and_ungroup.sql`, which §3 of the same handoff ties to issue #2494. The link from that migration file to PR #2423 is not established by the cited section; it rests on the merge-commit evidence recorded in the style-group-rebuild row of the table below.
The write-side query plans were **not** obtained: they were retired on the explicit permission-boundary record (the read-only role's `42501` refusal, call graph line 234, recorded as NOT MEASURED at line 237), not measured.
This closeout makes that durable evidence chain explicit; it does not re-run production writes or invent a new structural change.

The former Step 3 blockers #2213, #2214, and #2215 are closed. Their outcomes are retained below as evidence, not reopened by this programme.

## Baseline call graph

The authoritative first-stage artifact is [the 2026-09-04 call graph](../../database-efficiency-callers/2026-09-04T2130Z/call-graph.md), with its companion [machine-readable caller inventory](../../database-efficiency-callers/2026-09-04T2130Z/callers.json) and [definition retrieval record](../../database-efficiency-callers/2026-09-04T2130Z/function-definitions.sql).

It established the following without a production write:

- The reported 181-second `asset_effective_tags` population was a one-off backfill, not recurring work. The recurring cost was trigger churn.
- `refresh_style_group_counts_batch` rewrote unchanged rows, which re-fired the DAM search projection.
- `clear_style_group_batch` was keyset/index bounded; its high time was a downstream trigger cascade, not a missing scan index.
- `refresh_style_guide_matviews` had a fifteen-minute cron caller, no observed overlap, and a non-concurrent folders refresh because the then-current view lacked a qualifying unique index.
- The five target operations had named callers, schedules or triggers, concurrency descriptions, input bounds, and timestamped production-read-only cost observations. The named limits it recorded were the changed-row counts, the write-side plans that the read-only role correctly refused to execute (call graph lines 231 and 237), the per-statement split of the matview refresh mean (line 149), and the actual lock-wait time inflicted on readers of the folders matview (line 151). The last two remain unmeasured and are not closed by this closeout.

## Resolution of the named unknowns

**Provenance of the row-count figures below.** The `asset_effective_tags` write figures are recorded in this repository: over the common 6.853-day window the call graph's §5 table (line 247, and `callers.json`) records **8,808,328 inserts and 6,762,623 deletes** against 2,045,705 live rows. Deletes and inserts are not equal and are not "pairs"; the call graph's §2 characterises the pattern as delete/insert churn from a row trigger. The 10,864-of-10,868 already-correct groups is a different kind of figure: it is a production measurement quoted from the entry-gate comment on issue #2214 (also transcribed into the header of `supabase/migrations/20260905142725_style_group_counts_change_predicate.sql`). That measurement is **not independently reproducible from this repository** — nothing here re-derives it, and it is recorded as cited evidence rather than as a measurement taken by this closeout.

| Operation | What the original call graph left open | Subsequent governed resolution | Durable evidence |
|---|---|---|---|
| Effective-tag projection | Distinguish true changes from delete/reinsert churn and prove a safe no-op path. | The call graph records 8,808,328 inserts and 6,762,623 deletes on `asset_effective_tags` over the 6.853-day window (line 247), after the one-off backfill and with no net content change. PR #2399 changed only the assets branch to compare derived and stored sets, leaving an unchanged set unwritten. | [#2213 production measurement](https://github.com/u2giants/shared-db/issues/2213#issuecomment-5552480273); PR #2399 merge `22b6c2341169819491af8a18961b13bc4afb7b09`; `20260905143005_effective_tag_sync_set_comparison.sql` and its contract tests. |
| Style-group counts | Measure whether the nightly all-group call rewrote unchanged values. | The cited issue comment reports 10,864 of 10,868 groups already correct. PR #2397 added a complete null-safe change predicate, so unchanged groups are not updated and therefore do not re-fire the search projection. | [#2214 production measurement](https://github.com/u2giants/shared-db/issues/2214#issuecomment-5552465955); PR #2397 merge `da2335d6603a41e36119113b128a5a3323a134cd`; `20260905142725_style_group_counts_change_predicate.sql` and its contract tests. |
| Style-group rebuild | Identify a remaining write-side cause without assuming a scan problem. | A separately measured follow-on found whole-table re-stamping of already-correct assignments and a non-convergent ungroup case. PR #2423 guarded unchanged assignments and upserts and added the bounded ungroup path. The repository record linking the plan's "#2408 (guarded rebuild)" citation to this PR is the merge commit itself: `cb085f37a68296ebe04ea25e5ee743d2c0a21f2e` is titled "Merge pull request #2423 from u2giants/claude/issue-2408-style-group-rebuild" and changes only `20260906035323_style_group_rebuild_guard_and_ungroup.sql` and its contract file, and that migration file is headed "Issue #2408". | `20260906035323_style_group_rebuild_guard_and_ungroup.sql` and its contract tests; PR #2423 merge `cb085f37a68296ebe04ea25e5ee743d2c0a21f2e`. |
| Style-guide matviews | Prove the unique-index/freshness conditions before changing a blocking refresh. This operation is a full refresh, so it never carried a changed-row question. | The qualifying folders unique index and the concurrent refresh of both matviews landed **inside the #2212 migration**, delivered by PR #2339 (title: bounded crawl reconciliation, PDF text, and unified search (#2212); merged 2026-09-05). #2215 therefore had no separate structural change of its own and was closed on that evidence; this row must not be read as #2215 delivering its own migration. The #2215 read-only closeout comment proves both qualifying unique indexes are valid and that `CONCURRENTLY` is legal for each, and it measures consumer read frequency and source-change frequency. **No aggregate-drift verification artifact exists in this repository**, so no drift result is claimed here. The residual cadence is application scheduling and was retained because it was draining the bounded search-document backlog. | [#2215 closeout](https://github.com/u2giants/shared-db/issues/2215#issuecomment-5552470381); PR #2339 merge `9e83aa0460517613b44fd4bdf553b55f299b19ae`; `20260905104802_popsg_bounded_reconcile_pdf_text_and_search.sql`, headed "Issue #2212", and its contract tests. |
| Style-group clear | Measure whether the keyset-bounded clear rewrites unchanged rows. | **Not resolved.** No change in this chain measured `clear_style_group_batch`'s changed-row count; the rebuild migration cited above replaces `rebuild_style_groups_batch` only and notes that the clear function is on no cron schedule. | Call graph line 545 records this changed-row count as NOT MEASURED, and line 550 states the gate is **not** met for changed-row counts. This remains open; closing it needs preview instrumentation (Step 4/5). |

## What is still not proven

These items are carried forward as named unknowns, in the same style as the Step 1 and Step 2 records. None of them is remediated, scheduled, or converted into a proposed fix here.

- **The embedding lease/error path was never traced.** The Step 3 deliverable list in `plan_database_efficiency_and_api_security.md` line 235 asks for "any job that rebuilds `dam_search_documents` or repeatedly updates embedding leases/errors". That object class exists and is deployed (`public.claim_dam_search_embedding_documents` and `public.mark_dam_search_embedding_error`, defined in `supabase/migrations/20260825031841_popdam_ai_search_forward_recovery.sql`). A search of the whole cited artifact directory `docs/verification/database-efficiency-callers/2026-09-04T2130Z/` for `embedding`, `lease` or `WAL` returns zero matches. **NOT TRACED.**
- **WAL delta and transaction duration were not recorded.** The same plan line 237 asks for transaction duration, locks, WAL delta and dead-tuple delta. The call graph's query log records dead tuples but contains no WAL query and no transaction-duration measurement. **NOT MEASURED.**
- **`clear_style_group_batch`'s changed-row count** (call graph line 545). **NOT MEASURED.**
- **Write-side query plans** for `clear_style_group_batch`, `rebuild_style_groups_batch` and `refresh_style_group_counts_batch`. Retired on a recorded permission-boundary refusal, not measured (call graph lines 231 and 237). **NOT MEASURED.**
- **Per-statement split of the matview refresh mean** (call graph line 149) and **actual lock-wait time on readers of the folders matview** (line 151). **NOT MEASURED.**

This closeout certifies against the Step 3 verification gate at `plan_database_efficiency_and_api_security.md` line 243 **in explicit partial scope, not in full**. Of that gate's seven requirements, five — named caller, trigger/schedule, input size, concurrency pattern and timestamped cost — are met for all five operations. Two are **not** met as literally written: the changed-row count is NOT MEASURED for `clear_style_group_batch` (call graph line 545), and the query-plan requirement is unmet because no write-side plan was obtained for any of the three write functions (call graph lines 231 and 237, retired on the read-only role's recorded `42501` refusal) and because `refresh_style_guide_matviews` and `sync_asset_effective_tags` carry no query plan at all (call graph line 546). The call graph reaches the same conclusion in its own words at line 550: the gate "is **not** met for changed-row counts, and the write-side plans are blocked by the read-only boundary". This closeout does not claim the literal gate text is satisfied for every requirement, and it does not claim the wider instrumentation list at lines 235-237 was completed.

## What this does and does not authorize

- The evidence satisfies Step 3 to the partial scope certified above — five of the gate's seven requirements for all five operations, changed-row counts resolved for three of the five, and the changed-row count for `clear_style_group_batch` plus every write-side query plan still open. It supports the already-landed, narrowly scoped changes above, whose separate migration files, contract tests, pull requests and merge commits are cited in the table; their preview, promotion, and production-authorization records are held elsewhere and are neither examined nor certified here.
- It does **not** make a further projection, scheduling, index, or policy change automatic. Step 4 remains the application-owned work gate; Steps 5–8 keep their own issue, preview, review, and production-authorization requirements.
- Issue #2662 remains open and unscheduled. Its documented cross-application read path is unrelated to this Step 3 closeout and is not remediated here.

## Acceptance check

All five Step 3 operations have a named caller/trigger or schedule, input context, concurrency pattern, and a timestamped cost source. Query-plan coverage is narrower and is **not** claimed for all five: `clear_style_group_batch` and `refresh_style_group_counts_batch` have a read-side plan plus an explicit permission-boundary record for the refused write side, `rebuild_style_groups_batch` has a read-side plan only, and `refresh_style_guide_matviews` and `sync_asset_effective_tags` have no query plan at all (call graph line 546). Changed-row questions were resolved for three of the five: `refresh_style_guide_matviews` is a full refresh and never carried a changed-row question, and `clear_style_group_batch` remains **NOT MEASURED** and is carried above and below as an open unknown. Where a later change was justified, the precise migration, contract tests, PR, and merge commit are recorded above. No unknown was silently converted into a proposed fix.
