import test from'node:test';import assert from'node:assert/strict';import path from'node:path';import{fileURLToPath}from'node:url';import{validateAll,validateEntry,median}from'./ledger-lib.mjs';const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'../..');
test('repository ledger and fixture links validate',()=>assert.ok(validateAll(root).entries.length>=1));test('filename and id must agree',()=>assert.throws(()=>validateEntry({file:'x.json'}),/missing|mismatch/));
test('duration metrics reject corrupt values',()=>{const base={file:'blk_00000000000000000000000000000000.json',id:'blk_00000000000000000000000000000000',opened_at:null,resolved_at:null,first_red_check_at:null,diagnosis_proved_at:null,class:'other',issue_or_pr:'x',guard:'x',symptom:'x',proof_command:'x',resolution:null,fixed_by:null,corpus_fixture:null,active_minutes_lost:2,wall_minutes_blocked:1,estimate:false};assert.throws(()=>validateEntry(base),/cannot exceed/);assert.throws(()=>validateEntry({...base,active_minutes_lost:-1,wall_minutes_blocked:null}),/non-negative/)});
test('even-sized median averages both middle values',()=>assert.equal(median([1,3,9,11]),6));
