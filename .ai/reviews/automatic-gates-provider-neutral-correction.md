Correction: reject every durable GLM/Z.ai/model-specific requirement in your plan. GLM is only this session's worker. The workflow, docs, tests, environment config, secrets, comments, and review contract must be provider- and model-neutral. Use generic independent-review evidence with explicit APPROVE verdict, reviewer identity/type, exact SHA and allowlist binding. Do not pin a provider/model.

Also, `20260810140000` and B3 are already applied to production, so do not require applying either as a canary. This task must not touch Supabase production at all. The replacement automatic gates should be proven offline/CI and through production dry-run only before the narrow Environment API change, per the owner's explicit authorization.

Reissue only the corrected implementation plan. Resolve the clean/current PR-state gate robustly for this repo's merge strategy; avoid untestable fallback claims. Make preview evidence enforceable without depending on unavailable workflow-dispatch inputs, perhaps a committed/immutable evidence contract bound to SHA+allowlist and verified by a script. Identify exact files/tests and API sequencing. Return APPROVE/REQUEST CHANGES. No tools.
