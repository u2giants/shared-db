{
  "schema_version": 1,
  "generation": 8,
  "work_issue": 2579,
  "work_type": "structural",
  "route": "exclusive-db-lane",
  "goal": "Expose complete all-licensor Property-source coverage in DB Data Admin: land the Paramount TrackerPlus submission landing schema, classify the post-inventory plm.marvel_* and plm.wwe_* families in api.source_capture_inventory and give TrackerPlus its own latest-complete capture clock, and add exactly three source groups to api.db_data_admin_scraped_properties (Paramount Submissions (TrackerPlus), Coca-Cola Creative asset-library Property choices, WWE Submissions) while keeping Paramount Creative a separate group and manufacturing no vocabulary from inferred or secondary evidence. Publish config/db-data-admin-property-source-coverage.json as the machine-readable authority with a fail-closed validator, and bind the in-place routine rewrite with a hash-bound production verification sidecar and a verifier-owned catalog contract.",
  "base_sha": "e0962e66d6d1760fe70a4ede9d2caae048d61194",
  "dispatcher": "claude shared-db orchestrator marker 2669",
  "worker": "claude issue 2579 source coverage",
  "branch": "claude/issue-2579-source-coverage",
  "worktree": "C:/repos/shared-db-worktrees/issue-2579-source-coverage",
  "allowed_paths": [
    ".agent/completion.json",
    ".agent/contract.json",
    ".github/workflows/shared-supabase-migrations.yml",
    "config/db-data-admin-property-source-coverage.json",
    "docs/verification/throughput-guard-truth-audit-20260828.json",
    "docs/verification/throughput-guard-truth-baseline-20260828.json",
    "scripts/check-db-data-admin-property-source-coverage.mjs",
    "scripts/check-db-data-admin-property-source-coverage.test.mjs",
    "scripts/production-verification-sidecars/20260910155753.json",
    "scripts/production_business_risk_gate.py",
    "scripts/production_catalog_verification.py",
    "supabase/migrations/20260910155753_all_licensor_property_source_coverage.sql",
    "supabase/tests/peanuts_landing_contracts.sql",
    "supabase/tests/pmt_trackerplus_submission_landing_contracts.sql",
    "supabase/tests/sega_landing_contracts.sql",
    "supabase/tests/wildbrain_landing_contracts.sql"
  ],
  "file_writes": [
    ".agent/completion.json",
    ".agent/contract.json",
    ".github/workflows/shared-supabase-migrations.yml",
    "config/db-data-admin-property-source-coverage.json",
    "docs/verification/throughput-guard-truth-audit-20260828.json",
    "docs/verification/throughput-guard-truth-baseline-20260828.json",
    "scripts/check-db-data-admin-property-source-coverage.mjs",
    "scripts/check-db-data-admin-property-source-coverage.test.mjs",
    "scripts/production-verification-sidecars/20260910155753.json",
    "scripts/production_business_risk_gate.py",
    "scripts/production_catalog_verification.py",
    "supabase/migrations/20260910155753_all_licensor_property_source_coverage.sql",
    "supabase/tests/peanuts_landing_contracts.sql",
    "supabase/tests/pmt_trackerplus_submission_landing_contracts.sql",
    "supabase/tests/sega_landing_contracts.sql",
    "supabase/tests/wildbrain_landing_contracts.sql"
  ],
  "db_reads": [
    "plm.pmt_property",
    "plm.coke_asset_property_option",
    "plm.coke_capture",
    "plm.wwe_property",
    "plm.wwe_submission_capture"
  ],
  "db_writes": [
    "plm.pmt_trackerplus_submission_capture",
    "plm.pmt_trackerplus_submission_property",
    "plm.begin_pmt_trackerplus_submission_capture",
    "plm.load_pmt_trackerplus_submission_capture_chunk",
    "plm.finalize_pmt_trackerplus_submission_capture",
    "api.source_capture_inventory_exact",
    "api.source_capture_inventory",
    "api.db_data_admin_scraped_properties"
  ],
  "prohibited_actions": [
    "No preview or production apply, no workflow dispatch, no lane-manager run, no worktree reaper run",
    "No merge of this pull request by the worker, and never gh pr merge --admin",
    "Do not weaken, skip, edit or disable any workflow, guard script or required check",
    "Do not edit any migration other than 20260910155753, and never edit an already-applied migration",
    "Do not create any database object outside the eight claimed objects; a wider scope requires a new contract generation",
    "No licensed or private licensor data in any file, commit message or issue comment; this repository is public"
  ],
  "required_checks": [
    "bash scripts/check-sql.sh",
    "node scripts/check-production-verification-sidecars.mjs --base origin/main",
    "node scripts/check-db-data-admin-property-source-coverage.mjs",
    "node --test scripts/check-db-data-admin-property-source-coverage.test.mjs",
    "python -m pytest scripts/test_production_catalog_verification.py scripts/test_check_production_verification_sidecars.py scripts/test_migration_derivation.py scripts/test_production_business_risk_gate.py -q",
    "node scripts/agent-work-contract.mjs --validate-contract --contract-file .agent/contract.json",
    "git diff --check"
  ],
  "assumptions": [
    "Docker is unavailable on this machine, so the full supabase/tests suite cannot be replayed locally; the ephemeral-database CI job is the first from-empty replay of this migration.",
    "The 907-line body of api.db_data_admin_scraped_properties is restated by no migration, so the single dynamic-execution marker is a targeted in-place rewrite via pg_get_functiondef with fail-closed old-text and new-text assertions and a no-op re-run path. Its durable outcome is the routine body, which catalog contract all_licensor_property_source_coverage_v1 reads back after apply; the contract is a strict superset of dcp_stable_identity_property_match_v1 so the #2576 and #2449 assertions are carried forward.",
    "Changing scripts/production_catalog_verification.py changes the throughput-guard detector source, so docs/verification/throughput-guard-truth-baseline-20260828.json must have its detector_source_sha256 regenerated in the same commit; precedent commits c740c820 and 339e4dba did exactly this.",
    "Every verification sidecar and every file on the preview job's data surface must be individually pinned in PREVIEW_PRODUCER_PATHS in scripts/production_business_risk_gate.py. Adding the sidecar and the coverage manifest there is a registration the offline gate test demands, not a relaxation.",
    "scripts/check-sql.sh forbids migration verification from reading api.source_capture_inventory or plm.* rows, so the migration's own verification proves the new classification from the installed definition text and the LIVE classification is proved after apply by the catalog contract.",
    "Three existing landing contract tests (peanuts, sega, wildbrain) carry a v_map prefix array whose own error text instructs a new landing to add one row. The plm.marvel_* and plm.wwe_* rows are appended below every pre-existing prefix, and no earlier prefix is a prefix of marvel_ or wwe_, so no existing classification can change meaning.",
    "The reserved migration version 20260910155753 was assigned by the dispatcher and is used exactly as given.",
    "An append-only grant guard must name the application roles it constrains. information_schema.role_table_grants also lists the table owner's own implicit UPDATE/DELETE/TRUNCATE, which no REVOKE in a migration can remove, so an unfiltered count can never reach zero. Generation 3 scopes the TrackerPlus mutating-grant assertion, and its contract test, to service_role, authenticated, anon and PUBLIC, the shape already used by the wildbrain and sega landing guards.",
    "Generation 4 repairs two defects the from-empty replay exposed once the migration applied. The in-place inventory rewrite was authored against the pre-20260909115140 definition and silently reverted the three append-only OPA capture tables to a current-snapshot count; the opa_capture root lateral and the latest-complete count, basis, status and note arms are restored ahead of the mutable opa_ family. The two new TrackerPlus read policies admitted only administrator and licensing, narrowing the Paramount read surface that #1249 forbids narrowing, so they now carry the same four-role staff set as every other plm.pmt_* read policy.",
    "Generation 5 is an evidence repin only, made by the shared-db orchestrator (marker 2689) and not by the worker. The branch was refreshed from origin/main at aef1504e7c3c7674a6a9709e42720b8f2a725f43 before review so the review diff would not invert main newer commits into deletions. That refresh made the previously reported implementation head 6aab066717b4bbc9de6132d502a9749977785d5e no longer a descendant of the checked PR base, which the git-evidence check refuses. No implementation file changed: the reported files_changed list is byte-identical to git diff --name-only aef1504e7c3c7674a6a9709e42720b8f2a725f43 836d0ffb22aed103cf80387ef1774a50637d22d1, and the reported implementation head simply moves to the update-from-main merge commit 836d0ffb22aed103cf80387ef1774a50637d22d1, above which only the two .agent evidence files change.",
    "Generation 6 is an evidence repin only, made by the shared-db orchestrator. The pull request conflicted with main solely in .agent/completion.json and .agent/contract.json, the two shared evidence paths every pull request writes. The branch was updated from the current main tip ef06ca5aec77ad4eccf0e3b40accc2c2faca2cd6 and both conflicts were resolved by keeping this pull request own evidence; main version described already-merged work. No non-.agent file conflicted and no implementation file changed: files_changed is byte-identical to git diff --name-only ef06ca5aec77ad4eccf0e3b40accc2c2faca2cd6 38f1c7cd3657bb8932644ea86464666e82c7b17d, and the reported implementation head moves to the update-from-main merge commit 38f1c7cd3657bb8932644ea86464666e82c7b17d, above which only the two .agent evidence files change.",
    "Generation 7 answers the grok-4.6 REVISE on this pull request with four in-place fixes and no new migration, because 20260910155753 is applied nowhere yet. (1) The coverage validator now enforces the non-empty-family rule on the CI path, where it previously did not run at all; the derived catalog's only real blind spot, a table older than this repository's migration history, is handled by an explicit, reason-bearing, self-invalidating per-family declaration rather than by skipping the rule, and only the samples family (plm.sample_*) needs it. (2) The in-place RPC rewrite now counts each of its three anchors and refuses anything but exactly one occurrence, so a duplicated anchor can no longer be rewritten in every copy while still satisfying the new-text checks and contradicting the sidecar's uniqueness claim. (3) Every refusal block in the TrackerPlus landing contract test now catches SQLSTATE 'P0001' only; all eight refusals it exercises are raised with that errcode, including the remapped unique_violation, so nothing it previously proved stops being proved while a missing function or an unrelated error no longer counts as a refusal. (4) Status 'abandoned' is removed from the capture CHECK rather than given a writer: no function ever wrote it, removal is the smaller change, and the CHECK now names exactly the states the loaders produce. Sibling landing tables keep 'abandoned' as a documented dead state; this table does not repeat that. The hash-bound sidecar was regenerated for the edited migration (new migration_sha256, marker review range 1095-1147 for the single marker now at line 1144, rationale extended to state the occurrence-count assertion). No guard, workflow, check or test assertion was weakened or deleted.",
    "Generation 8 is an evidence repin only, made on the shared-db orchestrator instruction after the governed APPROVE at head b0f3ab8fc6adc8620f02885e268dbabfce4e8eeb. Main moved to e0962e66d6d1760fe70a4ede9d2caae048d61194 (PR #2684, docs and .agent evidence only). The pull request conflicted with main solely in .agent/completion.json and .agent/contract.json, resolved by keeping this pull request own evidence; no non-.agent file conflicted and no implementation file changed. git diff b0f3ab8fc6adc8620f02885e268dbabfce4e8eeb against the merge commit, excluding .agent, shows only the two #2684 documentation files, and the reported implementation head moves to the update-from-main merge commit 3e81a7a35bd70651919b61d20abb70ebb8c6ba91, above which only the two .agent evidence files change."
  ],
  "stop_conditions": [
    "A required check proves a real defect in the migration or its contract test",
    "The migration would have to be edited after the hash-bound sidecar is published without regenerating it",
    "The pull request base moves so that the reported files_changed no longer matches Git",
    "Any check failure could only be cleared by weakening a guard, a workflow or a required check",
    "The work would require creating a database object outside the eight claimed objects"
  ]
}
