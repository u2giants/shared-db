{
  "allowed_paths": ["scripts/run-governed-review.test.mjs", ".agent/contract.json", ".agent/completion.json"],
  "assumptions": [
    "run-governed-review.mjs locates the codex report by taking the last non-empty line of wrapper stdout, so any line printed after that path breaks every codex review.",
    "The abandoned ai-devops commit 644341f5 would have appended exactly such a line, and nothing in this repository's tests named that hazard.",
    "This is test-only repository maintenance: no database object, migration, preview, or production behaviour changes."
  ],
  "base_sha": "2dfd6de555ac47d88375c2b8f6987a50e4056041",
  "branch": "claude/issue-2307-codex-verdict-env",
  "db_reads": [],
  "db_writes": [],
  "dispatcher": "claude repo-maintenance session working the oldest ready repo-maintenance queue",
  "file_writes": ["scripts/run-governed-review.test.mjs", ".agent/contract.json", ".agent/completion.json"],
  "generation": 1,
  "goal": "Pin, as a test, that nothing may follow the codex report path on wrapper stdout, so the abandoned stdout-verdict fix cannot be re-landed and silently kill the codex reviewer.",
  "prohibited_actions": [
    "No change to scripts/run-governed-review.mjs or any other production script",
    "No migration, preview apply, reviewer allocation, merge of other work, production promotion, or shared-database write",
    "No relaxation of any existing verdict, head-binding, or preservation rule",
    "No object or path outside this contract"
  ],
  "required_checks": ["node --test scripts/run-governed-review.test.mjs", "git diff --check"],
  "route": "repo-maintenance",
  "schema_version": 1,
  "stop_conditions": [
    "The regression cannot be expressed without changing production code",
    "A competing change touches scripts/run-governed-review.mjs codex report resolution",
    "Implementation requires a path outside this contract"
  ],
  "work_issue": 2307,
  "work_type": "repo-maintenance",
  "worker": "claude issue2307-repo-session",
  "worktree": "C:/repos/shared-db-worktrees/issue-2307-codex-verdict-env"
}
