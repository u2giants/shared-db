{
  "schema_version": 1,
  "work_issue": 2311,
  "work_type": "repo-maintenance",
  "route": "repo-maintenance",
  "goal": "Make the two lane-script refusals named in issue #2311 state the condition they actually failed on. A closed work issue with an open pull request was reported as a head change that had not happened, and a verdict recorded against an orchestrator-marker number was reported as a lease the reviewer did in fact hold. Append a specific cause to each, keeping the existing message prefixes so prior assertions and operator runbooks still match, and issue no fresh wire read from any error path.",
  "base_sha": "71fe95d64a7e6ff1e4d822145271853a31d82b2d",
  "dispatcher": "albert via claude session, repo-maintenance route (outside the structure/schema orchestrator, AGENTS.md 0.0-C)",
  "worker": "claude repo-maintenance session working the five oldest unblocked non-orchestrator issues",
  "branch": "claude/issue-2311-refusal-causes",
  "worktree": "C:/repos/shared-db/.claude/worktrees/cleanup-worktree-f356f1",
  "allowed_paths": [
    "scripts/manage-migration-author-lanes.mjs",
    "scripts/manage-migration-author-lanes.test.mjs",
    ".agent/contract.json",
    ".agent/completion.json"
  ],
  "file_writes": [
    "scripts/manage-migration-author-lanes.mjs",
    "scripts/manage-migration-author-lanes.test.mjs",
    ".agent/contract.json",
    ".agent/completion.json"
  ],
  "db_reads": [],
  "db_writes": [],
  "prohibited_actions": [
    "No database change of any kind: no migration, no schema edit, no preview apply, no production promotion",
    "No change to any refusal CONDITION; only the text each refusal reports may change",
    "No fresh GitHub wire read from an error path, which would spend the fixed 25-request review budget and could replace the real cause with its own",
    "No weakening or deletion of an existing test or guard",
    "No edit outside allowed_paths"
  ],
  "required_checks": [
    "node --test scripts/manage-migration-author-lanes.test.mjs",
    "git diff --check"
  ],
  "assumptions": [
    "scripts/manage-migration-author-lanes.mjs is a PROTECTED_SOURCE_PATH; no other open pull request touched it when this branch was cut, verified against all eight open PRs",
    "The two named traps are the ones in issue #2311; both string sites were located on origin/main before editing and neither had been repaired",
    "Appending to the message keeps the prefix intact, so the seven existing tests matching /changed after mutex acquisition/ remain valid coverage rather than being rewritten to fit",
    "reviewTargetEligible costs wire requests through requireReviewWireCapacity, so the cause helpers deliberately re-inspect only already-fetched objects instead of re-asking",
    "This is repo-maintenance under AGENTS.md 0.0-C, owned by a separately started repository session, not routed to the structure/schema orchestrator"
  ],
  "stop_conditions": [
    "Any existing test in scripts/manage-migration-author-lanes.test.mjs fails",
    "Another open pull request begins touching scripts/manage-migration-author-lanes.mjs",
    "A required refusal condition, not just its wording, would have to change to satisfy the issue"
  ]
}
