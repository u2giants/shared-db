{
  "schema_version": 1,
  "generation": 1,
  "work_issue": 2769,
  "work_type": "structural",
  "route": "shared-db-orchestrator",
  "goal": "Restrict EXECUTE on public.queue_nightly_rebuild_style_groups() so PUBLIC, anon and authenticated cannot run the #2440 rebuild-queue function; keep service_role and postgres (the cron caller).",
  "base_sha": "b68a1900a6c287bd16adcbdacc8a3e4a8db543b1",
  "dispatcher": "Claude orchestrator #2758",
  "worker": "claude-2758-2769",
  "branch": "claude/issue-2769-rebuild-queue-grants",
  "worktree": "C:/repos/shared-db-worktrees/issue-2769",
  "allowed_paths": [
    "supabase/migrations/20260911145659_restrict_queue_nightly_rebuild_style_groups_execute.sql",
    "supabase/tests/queue_nightly_rebuild_style_groups_execute_grants.sql",
    "supabase/tests/chain_style_group_counts_to_rebuild_completion_contracts.sql",
    ".agent/contract.json",
    ".agent/completion.json"
  ],
  "file_writes": [
    "supabase/migrations/20260911145659_restrict_queue_nightly_rebuild_style_groups_execute.sql",
    "supabase/tests/queue_nightly_rebuild_style_groups_execute_grants.sql",
    "supabase/tests/chain_style_group_counts_to_rebuild_completion_contracts.sql",
    ".agent/contract.json",
    ".agent/completion.json"
  ],
  "db_reads": [],
  "db_writes": [
    "function public.queue_nightly_rebuild_style_groups"
  ],
  "prohibited_actions": [
    "No direct production DDL; production changes only through the governed promotion workflow.",
    "No fabricated reviews or verdicts; no deleting locks, audit rows or history.",
    "Do not touch #2705, #2709, #2290, #2678 or the #2543 residual."
  ],
  "required_checks": [
    "bash scripts/check-sql.sh",
    "git diff --check"
  ],
  "assumptions": [
    "The only runtime caller is the nightly cron job running as postgres; no app (including popdam3) calls the function as anon or authenticated.",
    "Revoking authenticated does not change the function body or its signature."
  ],
  "stop_conditions": [
    "An application caller that runs as anon or authenticated is found.",
    "Exact object collision with another active claim."
  ]
}
