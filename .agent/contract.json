{
  "schema_version": 1,
  "work_issue": 2670,
  "work_type": "structural",
  "route": "shared-db-change",
  "goal": "Widen the bulk_operation_runs terminal-status vocabulary to the words production actually emits, retain the raw word in source_status, and decide success in one place: a generated succeeded column that treats 'completed' as a success only when the row declares progress->'failed' as JSON 0, with the failures index and the 'a failure must say why' constraint both reading that column. Generation 2 answers the governed review of PR #2674 (findings C1, H1, M1, M2, M3, L1-L5).",
  "base_sha": "1eaa20459253e54ecd7891b6f009eeb20c734f8d",
  "dispatcher": "shared-db orchestrator marker issue 2669",
  "worker": "claude sub-agent for issue 2670",
  "branch": "claude/issue-2670-bulk-run-status",
  "worktree": "C:/tmp/wt-2670-rev",
  "generation": 2,
  "allowed_paths": [
    "supabase/migrations/20260910123601_bulk_operation_runs_live_status_vocabulary.sql",
    "supabase/tests/bulk_operation_runs_contracts.sql",
    ".agent/contract.json",
    ".agent/completion.json"
  ],
  "file_writes": [
    "supabase/migrations/20260910123601_bulk_operation_runs_live_status_vocabulary.sql",
    "supabase/tests/bulk_operation_runs_contracts.sql",
    ".agent/contract.json",
    ".agent/completion.json"
  ],
  "db_reads": [],
  "db_writes": [
    "public.bulk_operation_runs",
    "public.bulk_operation_runs.status",
    "public.bulk_operation_runs.source_status",
    "public.bulk_operation_runs.succeeded",
    "public.bulk_operation_runs_failures_idx"
  ],
  "prohibited_actions": [
    "Do not apply this migration to preview or production",
    "Do not weaken, skip, or edit any workflow, guard, or CI check to make a check pass",
    "Do not edit files outside this worktree",
    "Do not dispatch a workflow, run the lane manager, or merge the pull request",
    "Do not claim database objects beyond those declared in db_writes"
  ],
  "required_checks": [
    "bash scripts/check-sql.sh",
    "node scripts/check-applied-migration-edit.mjs",
    "git diff --check"
  ],
  "assumptions": [
    "Migration version 20260910123601 is the version reserved by claim #2671 for this work; it is not applied anywhere, so it is edited in place rather than superseded.",
    "No Docker or local Postgres is available in this worktree, so supabase/tests contract tests cannot be executed locally; CI is their first real execution.",
    "No database credential is configured locally, so no live ledger or applied-migration comparison is possible from here.",
    "Claim #2671 has been expanded by the orchestrator to declare public.bulk_operation_runs_failures_idx.",
    "Generation 2 claims no object beyond generation 1: the same table, the same status/source_status/succeeded columns and the same index name. progress is a pre-existing column of 20260909202801 and is read, not added."
  ],
  "stop_conditions": [
    "A required check proves a genuine defect in the migration SQL or the contract tests",
    "The declared object set would have to widen beyond db_writes",
    "Making a check pass would require editing a workflow or guard",
    "The pull request base moves such that the reported evidence no longer describes the branch"
  ]
}
