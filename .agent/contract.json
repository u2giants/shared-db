{
  "schema_version": 1,
  "work_issue": 2715,
  "work_type": "repo-maintenance",
  "route": "repo-maintenance",
  "goal": "Add a fail-closed lightweight required-status path for prose-only pull requests without dispatching the database guarded-merge workflow or weakening behavior-changing changes.",
  "base_sha": "439beffc5970b60bc7e1c8e0445f892fae46f8a7",
  "dispatcher": "Codex issue 401 programme repo-maintenance owner",
  "worker": "Codex prereq_2715",
  "branch": "codex/issue-2715-docs-status-v2",
  "worktree": "C:/repos/shared-db-worktrees/issue-2715-docs-status-v2",
  "allowed_paths": [
    ".agent/contract.json",
    ".agent/completion.json",
    ".github/workflows/documents-only-merge-authorization.yml",
    "AGENTS.md",
    "scripts/check-documents-only-merge-authorization.mjs",
    "scripts/check-documents-only-merge-authorization.test.mjs",
    "scripts/check-documents-only-pull-request.mjs",
    "scripts/documents-only-merge-authorization.test.mjs",
    "scripts/lib/documents-only-change.mjs",
    "scripts/manage-migration-author-lanes.test.mjs",
    "scripts/write-documents-only-merge-status.mjs",
    "scripts/write-documents-only-merge-status.test.mjs"
  ],
  "file_writes": [
    ".agent/contract.json",
    ".agent/completion.json",
    ".github/workflows/documents-only-merge-authorization.yml",
    "AGENTS.md",
    "scripts/check-documents-only-merge-authorization.mjs",
    "scripts/check-documents-only-merge-authorization.test.mjs",
    "scripts/check-documents-only-pull-request.mjs",
    "scripts/documents-only-merge-authorization.test.mjs",
    "scripts/lib/documents-only-change.mjs",
    "scripts/manage-migration-author-lanes.test.mjs",
    "scripts/write-documents-only-merge-status.mjs",
    "scripts/write-documents-only-merge-status.test.mjs"
  ],
  "db_reads": [],
  "db_writes": [],
  "prohibited_actions": [
    "Do not modify a migration or any database state.",
    "Do not authorize an unreadable, mixed, executable, workflow, script, test, configuration, migration, or behavior-changing rulebook diff.",
    "Do not execute pull-request head code in the privileged status workflow.",
    "Do not bypass the shared merge and production-freeze lock."
  ],
  "required_checks": [
    "git diff --check",
    "node --test focused documents-only authorization and merge-lock tests",
    "node scripts/check-github-transport-conformance.mjs",
    "independent exact-head reviewer-safety review",
    "live prose-only required-status acceptance pull request"
  ],
  "assumptions": [
    "Issue 2715 is repo-maintenance outside the shared-db structure orchestrator.",
    "Plans and bounded declarative routing pointers are lightweight prose; behavior-changing instruction edits retain the full guarded path.",
    "The required status must be posted only while the same merge and production-freeze exclusion used by guarded merges is held."
  ],
  "stop_conditions": [
    "Stop if implementation requires a database or production write.",
    "Stop if the pull-request head must be checked out or executed by pull_request_target.",
    "Stop if an unknown diff can receive success.",
    "Stop if exact-head independent review is unavailable."
  ],
  "generation": 1
}
