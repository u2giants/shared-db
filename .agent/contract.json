{
  "schema_version": 1,
  "work_issue": 2724,
  "generation": 4,
  "work_type": "structural",
  "route": "shared-db-orchestrator",
  "goal": "Make plm.\"FactoryTime\" a named, described, tagged lead-time template and add plm.product_type_factory_time so DesignFlow Production Tracking can assign one template per product type (mgCategory, MG01, MG02), matching the object names the consuming app already uses.",
  "base_sha": "26d9345b3a0ee015d8d02c0705002dd10b7f3f4e",
  "dispatcher": "claude shared-db orchestrator marker #2758",
  "worker": "claude issue-2724 migration author lane (claim #2803)",
  "branch": "claude/issue-2724-factory-time",
  "worktree": "C:/repos/shared-db/.claude/worktrees/recursing-poincare-85d3a9",
  "allowed_paths": [
    ".agent/completion.json",
    ".agent/contract.json",
    "config/db-data-admin-property-source-coverage.json",
    "supabase/migrations/20260911214438_plm_factory_time_templates.sql",
    "supabase/tests/plm_factory_time_template_contracts.sql"
  ],
  "file_writes": [
    ".agent/completion.json",
    ".agent/contract.json",
    "config/db-data-admin-property-source-coverage.json",
    "supabase/migrations/20260911214438_plm_factory_time_templates.sql",
    "supabase/tests/plm_factory_time_template_contracts.sql"
  ],
  "db_reads": [],
  "db_writes": [
    "table plm.\"FactoryTime\"",
    "column plm.\"FactoryTime\".name",
    "column plm.\"FactoryTime\".description",
    "column plm.\"FactoryTime\".tags",
    "column plm.\"FactoryTime\".updated_by",
    "column plm.\"FactoryTime\".product_subtype",
    "index plm.factory_time_name_ci_key",
    "table plm.product_type_factory_time",
    "column plm.product_type_factory_time.previous_factory_time_id",
    "index plm.product_type_factory_time_factory_time_id_idx"
  ],
  "prohibited_actions": [
    "Do not rename any object the consuming app already uses, including previous_factory_time_id.",
    "Do not write or migrate application rows; plm.\"FactoryTime\" and plm.product_type_factory_time rows belong to designflow-tracking.",
    "Do not touch the dflow or dflow_prod copies of FactoryTime.",
    "Do not run DDL directly against preview or production; apply only through the governed workflows.",
    "Do not widen an existing Property-source coverage family selector; add the one new family entry only.",
    "Do not quarantine supabase/tests/plm_factory_time_template_contracts.sql to make CI green."
  ],
  "required_checks": [
    "bash scripts/check-sql.sh",
    "node scripts/check-db-data-admin-property-source-coverage.mjs",
    "python -m unittest scripts/test_migration_derivation.py"
  ],
  "assumptions": [
    "plm.\"FactoryTime\" holds zero rows in production, so name NOT NULL can be added without inventing names; the migration raises rather than guessing if that has changed.",
    "Version 20260911214438 is the version reserved by author claim #2803.",
    "popcre/designflow-tracking#55 and popcre/designflow-frontend#182 are already aligned to these exact names.",
    "Generation 4 changes no scope. It refreshes onto main 26d9345b by MERGING main, not rebasing: a rebase rewrites history and voids the governed APPROVE, which is exactly what cost generation 2 its verdict. Main's advance touched only the two .agent evidence files, which every lane overwrites; all three implementation files are untouched, so the pull request's own diff is byte-identical at the approved head b5a98122 and the merge head, and the approval carries forward under the rule merged in PR #2780."
  ],
  "stop_conditions": [
    "Stop if plm.\"FactoryTime\" is found to hold rows in preview or production.",
    "Stop if any object would have to be renamed away from what the consuming app expects.",
    "Stop if a guard refusal could only be cleared by weakening the guard."
  ]
}
