{
  "schema_version": 1,
  "work_issue": 2796,
  "work_type": "repo-maintenance",
  "route": "repo-maintenance",
  "goal": "Make the stored preview dispatch instruction name its dispatch mode, so a write route cannot silently dry-run. The producer attaches mode_sequence to the ready record AFTER both digests are taken, deliberately leaving the digests unchanged; mode_sequence is carried for a later phase and no validator refuses a manifest that omits it today. The enforcement this change actually ships is in the production gate, whose bare test -n checks gain named REFUSED messages instead of dying with only an exit code.",
  "base_sha": "bfdb19c916d2f481f1b740d5ce1318b065c13045",
  "dispatcher": "shared-db orchestrator (marker #2758)",
  "worker": "shared-db orchestrator, adopting the cold claude/issue-2796-preview-mode-apply lane",
  "branch": "claude/issue-2796-preview-mode-apply",
  "worktree": "C:/repos/shared-db/.claude/worktrees/issue-2796",
  "allowed_paths": [
    ".agent/contract.json",
    ".agent/completion.json",
    ".github/workflows/shared-supabase-migrations.yml",
    "docs/verification/throughput-guard-truth-audit-20260828.json",
    "scripts/check-workflow-preview-mode-guard.test.mjs",
    "scripts/manage-migration-author-lanes.mjs",
    "scripts/manage-migration-author-lanes.test.mjs",
    "scripts/orchestrator-flow/reconcile.mjs",
    "scripts/orchestrator-flow/reconcile.test.mjs"
  ],
  "file_writes": [
    ".agent/contract.json",
    ".agent/completion.json",
    ".github/workflows/shared-supabase-migrations.yml",
    "docs/verification/throughput-guard-truth-audit-20260828.json",
    "scripts/check-workflow-preview-mode-guard.test.mjs",
    "scripts/manage-migration-author-lanes.mjs",
    "scripts/manage-migration-author-lanes.test.mjs",
    "scripts/orchestrator-flow/reconcile.mjs",
    "scripts/orchestrator-flow/reconcile.test.mjs"
  ],
  "db_reads": [],
  "db_writes": [],
  "prohibited_actions": [
    "Do not hand-edit call_site_count or call_site_sha256 in the throughput truth audit, and do not recompute the digest by reasoning: it is JSON.stringify over discover() order, and two plausible alternatives (sorted keys, newline join) both produce wrong digests.",
    "Do not invent or reword a call-site disposition to clear a refusal; every site owes a hand-written reviewed judgement.",
    "Do not rebase or force-push this branch: it was authored by another session and is refreshed by merge only.",
    "Do not weaken any guard to make a check pass."
  ],
  "required_checks": [
    "node scripts/check-throughput-truth-audit.mjs",
    "node scripts/orchestrator-flow/reconcile.test.mjs",
    "node scripts/manage-migration-author-lanes.test.mjs",
    "node scripts/check-workflow-preview-mode-guard.test.mjs"
  ],
  "assumptions": [
    "The implementation commits 1331ed7d and 225a7b62 were authored by the original #2796 lane; this session adopted the branch after it went cold at 19:31 EDT with no open claim, no issue comments and a CONFLICTING state, and added only the merge from main plus this evidence pair.",
    "The branch carries no migration, so no version reservation or author lease is at stake.",
    "The truth-audit conflict was an append-versus-append: main added five sites and this branch two, with no semantic_key appearing on both sides, so the union required no re-judging.",
    "Issue #2796 had no db-work-scope block at all, which is why it was never dispatchable; it was classified repo-maintenance / reviewer-tooling by the orchestrator before this work was landed."
  ],
  "stop_conditions": [
    "Stop if any truth-audit call site cannot be carried forward mechanically and would need a newly invented disposition.",
    "Stop if resolving the conflict would require choosing one side's count over the other rather than re-deriving.",
    "Stop if the original lane shows signs of life on this branch."
  ],
  "generation": 2
}
