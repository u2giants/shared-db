{
  "schema_version": 1,
  "generation": 4,
  "work_issue": 2507,
  "work_type": "structural",
  "route": "shared-db-orchestrator",
  "goal": "Recover the inherited PopSG PDF v2 terminal-outcome change under the current orchestrator, preserving its behavior while re-reserving its migration after current-main reconciliation.",
  "base_sha": "71dca7b3deae7eea2ab0c2e47a320e1291dc0580",
  "dispatcher": "codex orchestrator marker 2714",
  "worker": "codex finish_2507 successor",
  "branch": "codex/issue-2507-popsg-pdf-v2",
  "worktree": "C:/repos/shared-db-worktrees/finish-2507-2714",
  "allowed_paths": [
    ".agent/contract.json",
    ".agent/completion.json",
    "supabase/migrations/20260911042655_popsg_pdf_terminal_outcomes.sql",
    "supabase/tests/popsg_pdf_terminal_outcome_contracts.sql"
  ],
  "file_writes": [
    ".agent/contract.json",
    ".agent/completion.json",
    "supabase/migrations/20260911042655_popsg_pdf_terminal_outcomes.sql",
    "supabase/tests/popsg_pdf_terminal_outcome_contracts.sql"
  ],
  "db_reads": [
    "table public.style_guide_files",
    "function public.claim_style_guide_pdf_text"
  ],
  "db_writes": [
    "table public.style_guide_pdf_text",
    "function public.complete_style_guide_pdf_text_v2",
    "table public.style_guide_search_documents"
  ],
  "prohibited_actions": [
    "Do not start an extraction workload, corpus rebuild, or data backfill.",
    "Do not change or remove the existing complete_style_guide_pdf_text RPC.",
    "Do not broaden grants beyond service_role or alter unrelated queue work.",
    "No merge, preview, or production write by worker; parent orchestrator serializes these stages."
  ],
  "required_checks": [
    "bash scripts/check-sql.sh",
    "supabase test db supabase/tests/popsg_pdf_terminal_outcome_contracts.sql",
    "node scripts/agent-work-contract.mjs --validate-contract --contract-file .agent/contract.json",
    "git diff --check"
  ],
  "assumptions": [
    "Migration 20260905104802 remains the base PDF contract and its existing completion RPC remains callable for staggered clients.",
    "The sanctioned claim supersession from 20260908134917 to 20260909162929 changes only the reserved migration identity, not the SQL behavior.",
    "A failed result remains transiently reclaimable under the existing maximum-attempt policy, while skipped and extracted results remain terminal until content identity changes.",
    "Generation 3 refresh preserves existing implementation ancestry and byte-identical SQL/tests while reconciling only shared evidence files against current main. Claim 2578 renewed by sanctioned manager.",
    "Current successor verified old and original versions absent from both live ledgers before guarded supersession to 20260911042655; SQL is unchanged."
  ],
  "stop_conditions": [
    "Stop if the implementation requires changing claim_style_guide_pdf_text or any database object outside claim 2578.",
    "Stop if existing rows cannot be preserved without a backfill or destructive rewrite.",
    "Stop before preview, external review, merge, or production promotion until the orchestrator explicitly releases that stage."
  ]
}
