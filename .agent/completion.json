{
  "schema_version": 1,
  "work_issue": 2311,
  "outcome": "ready-for-merge",
  "pr": 2633,
  "migration_versions": [],
  "contract_ref": "refs/db-contracts/2311/1",
  "contract_sha256": "cf3199d92391d915e21d6e36623ebea9a12e016877dc754077d8b60bf07da31e",
  "head_sha": "1418482ee92fcd79824dea9dfe8934e8980f8e2a",
  "evidence_note": "Text-only repair to two refusal messages in scripts/manage-migration-author-lanes.mjs plus four regression tests. No refusal CONDITION changed, no database object touched, and no migration added. files_changed lists the pull request's non-.agent diff, which is what the Git evidence check compares against; head_sha names the implementation commit that this evidence pair reports on.",
  "files_changed": [
    "scripts/manage-migration-author-lanes.mjs",
    "scripts/manage-migration-author-lanes.test.mjs"
  ],
  "db_reads": [],
  "db_writes": [],
  "checks": [
    {
      "command": "node --test scripts/manage-migration-author-lanes.test.mjs",
      "exit_code": 0,
      "evidence": "tests 486, pass 486, fail 0, duration_ms 2162. The suite held 482 tests before this change and none was edited, deleted or relaxed; the four added tests are #2311 cases. Re-run at head 1418482e."
    },
    {
      "command": "git diff --check",
      "exit_code": 0,
      "evidence": "No whitespace errors."
    },
    {
      "command": "discrimination check: each added test also asserts the MISLEADING alternative is absent",
      "exit_code": 0,
      "evidence": "assert.doesNotMatch(/head is now/) on the closed-issue case, assert.doesNotMatch(/work issue #1769 is/) on the moved-head case, and assert.doesNotMatch(/the assignment for issue/) on the no-lease case. Without these a message that simply appended every cause at once would pass, which is the defect being repaired rather than a fix for it."
    },
    {
      "command": "node scripts/check-pr-source-collisions.mjs (equivalent manual read of PROTECTED_SOURCE_PATHS against every open PR)",
      "exit_code": 0,
      "evidence": "scripts/manage-migration-author-lanes.mjs is the single member of PROTECTED_SOURCE_PATHS. All eight open pull requests at branch-cut time (2631, 2627, 2607, 2583, 2557, 2527, 2526, 2425) were queried with `gh pr view <n> --json files`; none touches that path, so this branch takes the protected source uncontended."
    }
  ],
  "assumptions_resolved": [
    "Both trap sites named by issue #2311 were located on origin/main at 71fe95d6 and neither had been repaired: reviewIssueEligible/assertReviewRequestEligible for trap 1, and the active-lease comparison inside recordReviewVerdict for trap 2.",
    "The existing message prefixes are preserved, so the seven prior assertions matching /changed after mutex acquisition/ still pass unmodified and remain real coverage.",
    "Neither cause helper issues a wire read. reviewTargetEligible charges against the fixed 25-request review budget through requireReviewWireCapacity, and a budget refusal raised from an error path would replace the real cause with its own; the helpers therefore re-inspect only the issue, PR and requested head already fetched by the caller.",
    "reviewActiveLeaseCause parses the lease commit through the existing parseReviewCursor and falls back to naming the raw SHA if that commit is unreadable, so an unparseable lease still produces a specific message rather than throwing a second error over the first.",
    "No refusal condition was changed. Every input that was refused before is still refused; only the reported reason is different.",
    "Repo-maintenance under AGENTS.md 0.0-C. No database read or write, no migration version reserved, and no author-lane claim required."
  ],
  "stop_conditions_hit": []
}
