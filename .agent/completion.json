{
  "schema_version": 1,
  "work_issue": 2802,
  "outcome": "ready-for-merge",
  "pr": 2814,
  "migration_versions": [
    "20260911224952"
  ],
  "contract_ref": "refs/db-contracts/2802/1",
  "contract_sha256": "8f3d34152b340470be46139ae6ae6fedbfeb8726568197329dcb2fdbcab32148",
  "head_sha": "c4537dc2013c691755ea9294af0fcdeb041c88c2",
  "files_changed": [
    ".agent/completion.json",
    ".agent/contract.json",
    "supabase/migrations/20260911224952_dam_style_guide_file_has_talent_likeness.sql"
  ],
  "db_reads": [],
  "db_writes": [
    "table dam.style_guide_file"
  ],
  "checks": [
    {
      "command": "bash scripts/check-sql.sh",
      "exit_code": 0,
      "evidence": "Static checks passed at merge head 69fed8b1. Guard B: no migration sorts before the base branch's newest version 20260911214438 (source: git:origin/main). Issue #1235 expected-count guard passed. Verify-cost guard passed: 1 added migration contains no prohibited verification read. Issue #1684 EOL reference guard passed. Guard B2 did not run: no live ledger source configured (informational, issue #651)."
    }
  ],
  "assumptions_resolved": [
    "Confirmed against the table definition in supabase/migrations/20260621151024_domain_tables.sql that dam.style_guide_file already carries title, relative_path, folder and metadata, so the raw source filename and label stay preserved separately and this migration does not touch them.",
    "The column is declared nullable with no default, so NULL remains a distinct third state meaning the source exposed no determination and is never a stand-in for FALSE.",
    "No database was read or written by this work; it is static authoring only. The contract permitted a read of core.style_guide that proved unnecessary, so db_reads is reported empty rather than widened to match the contract."
  ],
  "stop_conditions_hit": [],
  "evidence_note": "Refreshed onto main 485a0ba5 by MERGING main, never rebasing. The only conflict was the .agent evidence pair, which on main belongs to issue #2724; it was resolved to this pull request's own issue #2802 evidence and no implementation file was touched. Implementation head 69fed8b1; only .agent/completion.json follows it. The .agent pair is re-serialized together in a single commit after the merge head so both evidence files follow it, as the git-evidence rule requires; the contract reformat is whitespace only and its canonical hash is unchanged. Updated from main with a MERGE (never a rebase) at c4537dc2, resolving a conflict confined to the two .agent evidence files against lane #2817 by keeping the #2802 pair; the contract hash is byte-for-byte unchanged and the migration SQL was not touched. head_sha is re-pinned to this merge commit and files_changed recomputed against base 9eb1d587. This also mints a new reviewer-rotation sequence: the prior head a66b50ac carried a durable cursor naming muse-spark-1.3-contributor, whose wrapper cannot emit the governed verdict line the recorder requires, and assignWithMutexRetry returns the existing cursor for an unchanged issue/PR/head/slot. The allocator defect is filed as issue #2831. No verdict was hand-written, no terminal failure code invented, no reviewer excluded."
}
