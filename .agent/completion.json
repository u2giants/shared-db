{
  "schema_version": 1,
  "work_issue": 2318,
  "outcome": "ready-for-merge",
  "pr": 2636,
  "migration_versions": [],
  "contract_ref": "refs/db-contracts/2318/4",
  "contract_sha256": "a61e7f87489192fed46345a6cbdbd9c6f3c68dee48033102e35fc4fa4c9460ac",
  "head_sha": "0a6d3d008e492878af4d263fe40732433d4d701e",
  "evidence_note": "Generation 4. head_sha names 0a6d3d00, the last head at which every check other than the git-evidence file list was run; the only later commits are this evidence pair itself, which no report can name without naming its own SHA. The git-evidence checker compares files_changed against the whole PR diff including .agent/*, so the evidence pair itself is listed. Re-points the report at head a6fd30fa, the first update from main (origin/main after PR #2633 merged as e149bd0b). The .agent conflict against main was resolved in favour of this lane's own evidence pair; every non-.agent file is byte-identical to generation 2 at b0381830. Generation 2 added the throughput truth-audit dispositions for the three new source lines matching its `missing` pattern, which failed the Tools Offline Tests lane at generation 1 (run 34367211675). No SQL, no migration, no database access of any kind.",
  "files_changed": [
    ".agent/completion.json",
    ".agent/contract.json",
    "AGENTS.md",
    "docs/verification/throughput-guard-truth-audit-20260828.json",
    "scripts/check-orchestrator-marker.mjs",
    "scripts/check-orchestrator-marker.test.mjs",
    "scripts/lib/orchestrator-admission.mjs",
    "scripts/lib/orchestrator-admission.test.mjs",
    "scripts/lib/orchestrator-routing.mjs"
  ],
  "db_reads": [],
  "db_writes": [],
  "checks": [
    {
      "command": "node --test scripts/lib/orchestrator-admission.test.mjs",
      "exit_code": 0,
      "evidence": "19 tests, 19 pass, 0 fail. Covers each of the four real inferences behind marker #2312, every key in REFUSED_GROUNDS, casing/underscore/trailing-period variants, unrecognised free text, blank and missing, almost-right owner forms, both admissible forms, handover-issue agreement, the grandfathering warning, that grandfathering covers a MISSING field only, an unreadable creation date, and that renderRoutingBlock emits the field."
    },
    {
      "command": "node --test scripts/check-orchestrator-marker.test.mjs",
      "exit_code": 0,
      "evidence": "41 tests, 41 pass, 0 fail (35 pre-existing, 6 new). The new cases prove a db-work label, delegated task traffic, a structural need and a handoff each FAIL the guard; that an unauthorized marker is INVALID rather than NONE so the default stays non-orchestrator Path A; that explicit owner authorization passes and still resolves a target; that the live pre-admission marker warns but still routes; and that a marker both unroutable and unauthorized reports both causes rather than one."
    },
    {
      "command": "node --test scripts/lib/orchestrator-routing.test.mjs",
      "exit_code": 0,
      "evidence": "All pass. Combined run of the three suites: 60 tests, 60 pass, 0 fail. Pre-existing routing tests are unaffected because authorization was not added to REQUIRED_FIELDS."
    },
    {
      "command": "node --test scripts/manage-migration-author-lanes.test.mjs",
      "exit_code": 0,
      "evidence": "482 tests, 482 pass, 0 fail. The only other suite that imports the marker guard; unchanged by this work. Re-run at head a6fd30fa after the update from main, which brought in PR #2633's changes to that same script: combined run of all four suites is 546 pass, 0 fail."
    },
    {
      "command": "git diff --check",
      "exit_code": 0,
      "evidence": "No whitespace errors. Only CRLF advisory warnings from the repository's line-ending configuration."
    },
    {
      "command": "node scripts/check-throughput-truth-audit.mjs",
      "exit_code": 0,
      "evidence": "'truth audit OK: call_sites=272'. Three new call sites were reviewed and recorded as `excluded`, each with its own reason: two are refusal/warning strings about a missing `authorization:` routing-block field and one is a source comment about emitting that field blank. None asserts anything about a database object being missing or not applied, which is the audit's subject. The recorded count and semantic digest were regenerated from the discovered inventory, not hand-edited."
    }
  ],
  "assumptions_resolved": [
    "Admission cannot prevent a marker being opened, and the module header says so in its own words. What it does is make the grounds a required, typed, published field. The value is verified by the tests, not asserted: each of the four inferences that produced #2312 is proven to fail the guard.",
    "authorization is NOT in REQUIRED_FIELDS. Verified by the test that the live pre-admission marker still routes with a warning: adding the field to REQUIRED_FIELDS would have failed live marker #2629, which opened 2026-09-09 and is therefore NOT grandfathered by CONTRACT_EFFECTIVE_DATE 2026-08-27. The separate cutoff AUTHORIZATION_EFFECTIVE_DATE 2026-09-10 is what makes the change safe to land while an orchestrator is live.",
    "Grandfathering covers a missing field only. Verified by a dedicated test: a pre-cutoff marker declaring `db-work` still fails, and an unreadable createdAt is treated as in force.",
    "An unauthorized marker is INVALID, not NONE. Verified by resolving the target from the evaluated markers and asserting the refusing state, so the two opposite answers in the AGENTS.md exit table are not collapsed.",
    "The truth-audit dispositions are `excluded`, not `enriched`: these lines are prose about orchestrator admission and carry no throughput or verification claim. Verified by re-running the guard to a clean pass at 272 sites."
  ],
  "stop_conditions_hit": []
}
